var t = ' 1, 2, 3 '
var idx = 0
loop value in $t {
    # do something
    assert $value == $t[$idx]
    $idx += 1
}

assert $t[0] == 1

# assert out of bounds is none
# assert $t[3] == none


{
    var t = ' 1, 2, '3, 4' '
}
